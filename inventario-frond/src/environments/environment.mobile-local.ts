export const environment = {
  production: true,
  // Variante para probar en TU red local (no para compartir con otros).
  // Apunta a la IP actual de tu laptop en tu WiFi de casa. Si cambia tu IP
  // (otra red, o Windows te la reasigna), actualizá esto y volvé a
  // compilar con: ng build --configuration mobile-local
  apiUrl: 'http://192.168.1.6/api',
};
